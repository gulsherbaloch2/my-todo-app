# Boundaries for SDD Workflow Compliance

- MUST NOT enforce workflow if explicitly overridden by user
- MUST NOT halt progress due to workflow violations alone
- MUST NOT modify project structure to fit workflow
- MUST NOT make exceptions to workflow without user approval